# Local environment variables for dev only
# Copy this file to `.env` and fill in values.

# Server-only secret used to sign the session cookie issued by
# GET /api/auth/session. Use a long random string (32+ chars).
SESSION_SECRET=change-me-to-a-long-random-string

# LLM provider API keys. Optional if you pass `x-api-key` from the client,
# but recommended to set here for local development. The server route at
# /api/chat reads these from process.env during dev.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Note: Do NOT prefix secrets with VITE_. Variables prefixed with VITE_
# are exposed to the browser. Keep secrets server-only.

# Optional: If you ever need a client-exposed variable, prefix it with VITE_.
# VITE_SOME_PUBLIC_FLAG=

# Sentry (optional)
# Client DSN is safe to expose
# VITE_SENTRY_DSN=
# Server DSN (Cloudflare secret recommended)
# SENTRY_DSN=
# Auth token for sourcemap upload during build (do not commit)
# SENTRY_AUTH_TOKEN=
